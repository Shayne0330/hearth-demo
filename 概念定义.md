# Hearth 概念定义

## 1. 项目定位

Hearth 是一个面向多 IDE、多 Agent、多 Session 工作流用户的项目空间管理工具。

它的核心目标不是替用户保存所有信息，而是在用户回到某个 Session 时，通过空间关系、房间状态和过场提示，帮助用户快速恢复注意力和上下文记忆。

核心概念是“记忆宫殿”：把项目与 Session 做成可感知的空间实体，让用户以“回到房间”的形式恢复工作状态。

## 2. 核心对象关系

当前 MVP 采用以下叙事结构：

- 项目是房间。
- Session 是房间里的工作台。
- Agent / IDE 是房间的属性，而不是最主要的大标题。
- 同 Agent / IDE 下的项目房间在空间上更接近。
- 不强行推断项目之间的上下文承接关系，避免错误关联。

如果一个项目同时开了多个 Session，这个项目房间里会出现多张工作台。每张工作台代表一个 Session。

工作台上的台灯表达 Session 状态：

- Session 正在等待用户回看时，对应工作台台灯亮起。
- Session 结束并给出回复时，对应工作台台灯亮起。
- 从建筑外部看，房间整体亮灯。
- 展开成剖面沙盘后，用户可以看到更准确的亮灯工作台。

这个设计兼容两类使用习惯：

- 用户在同一个 Agent 中处理多个项目，每个项目有多个 Session。
- 用户在多个 Agent 中共同处理一个项目。

但 MVP 会优先服务第一种更常见、更符合当前个人工作流的场景。

## 3. 技术选型

MVP 推荐使用：

```txt
React / Vite / TypeScript
+ three
+ @react-three/fiber
+ @react-three/drei
```

选择理由：

- 当前仓库已经是 React + Vite，React Three Fiber 可以自然接入现有组件结构。
- Three.js 提供底层 3D 场景、相机、灯光、材质和点击拾取能力。
- React Three Fiber 让 3D 场景保持数据驱动，适合根据项目 / Session 数据生成空间结构。
- Drei 提供相机控制、HTML 标签、文本、几何辅助组件，能降低 MVP 开发成本。
- MVP 不需要第一人称漫游、复杂物理、复杂建模或真实游戏引擎能力。

暂不采用：

- CSS 3D：适合假 3D UI，但不适合复杂空间、遮挡、相机和点击拾取。
- Vanilla Three.js：控制力强，但和 React 状态机割裂，维护成本更高。
- Babylon.js：能力偏完整游戏 / 3D 引擎，对当前 MVP 偏重。
- Spline 或外部 3D 设计工具：适合展示，但不适合数据驱动的空间生成。

## 4. 视觉与交互形态

Hearth 的 3D 表现应服务于“空间记忆”，而不是做成游戏式进入房间。

MVP 的 3D 形态是：

- 2.5D / 轴测建筑立面。
- 可展开的建筑剖面沙盘。
- 以地图视角为主，不做第一人称进入房间。
- 不做 WASD 漫游。
- 通过短过场帮助用户完成心理切换。

## 5. 收起状态

收起状态下，Hearth 是一个小型 2.5D 建筑立面，作为透明背景的悬浮窗口，像一个小房子一样漂浮在用户的桌面或 IDE 上方。

它不是完整地图，而是一个状态仪表：

- 每个亮着的窗户对应一个项目房间。
- 同 Agent 的项目可以在同一层、同一侧翼或同一栋小楼里。
- 有 Session 回复或需要用户回看时，对应项目房间的窗户亮灯并轻微呼吸。
- 用户不需要展开时，它就是一个安静的小房子。

## 6. 展开状态

当某个项目房间亮灯，用户 hover 到小房子上时，小房子只做轻量预览，例如轻微放大、亮灯更明显或显示简短提示。

真正展开由点击触发。点击后，建筑从立面视图展开成剖面沙盘。

展开后的表现：

- 外立面像抽屉或书页一样打开。
- 楼层稍微错开，露出每个项目房间。
- 亮灯房间更突出。
- 展开后可以看到房间内具体哪张工作台亮灯。
- 点击亮灯房间或亮灯工作台即可进入对应项目 / Session。
- 点击时视图做一次短暂聚焦，同时显示“上一次目标 / 最近会话”等上下文提示。

过场动画不应过长。它的作用不是模拟用户走进房间，而是给用户一个短暂的心理缓冲：

```txt
我正在从当前工作状态切换到另一个项目状态。
```

## 7. MVP 开发策略

第一版不追求精细建模，采用程序化 3D 方案：

- 用几何体生成建筑、楼层、房间、窗户、走廊。
- 用材质、灯光、发光和透明度表达状态。
- 用简单几何体表达桌子、屏幕、便签、灯等房间内元素。
- 用 DOM overlay 或 Drei Html 显示项目名、上一次目标、最近会话等信息。
- 用相机和 mesh transform 完成立面到剖面沙盘的展开动画。

后续可以逐步升级为：

- 模块化低多边形资产。
- 更精致的建筑外立面。
- 更丰富的房间内物件。
- 更强的光照、材质和状态动效。

但 MVP 应避免一开始进入 Blender 建模、贴图烘焙和复杂资产管理流程。

## 8. 关键决策记录

- Agent / IDE 不再是最主要叙事对象，而是项目房间的属性。
- 项目是房间，是用户回到工作状态时最主要的空间锚点。
- Session 是房间里的工作台，用于恢复更具体的工作上下文。
- 一个项目可以包含多个 Session 工作台。
- 房间外部亮灯表示该项目内存在需要回看的 Session。
- 展开后亮灯工作台表示具体需要回看的 Session。
- 空间聚合暂时以同 Agent / IDE 为准。
- 不根据未知上下文关系自动连接项目，避免误导用户。
- 3D 交互以地图视角和建筑剖面为主。
- 不做第一人称进入房间动画。
- 不做 WASD 漫游。
- 收起状态是悬浮小房子，展开状态是建筑剖面沙盘。
- 技术路线采用 React Three Fiber + Three.js + Drei。
